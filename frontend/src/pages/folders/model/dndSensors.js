export const FOLDER_MOUSE_SENSOR_OPTIONS = {
  activationConstraint: {
    distance: 8,
  },
}

export const FOLDER_TOUCH_SENSOR_OPTIONS = {
  activationConstraint: {
    delay: 180,
    tolerance: 8,
  },
}
